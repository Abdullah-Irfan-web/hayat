<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Registration Information</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
    }

    .info-line {
      display: flex;
      margin-bottom: 10px;
      margin-top: 20px;
    }

    .label {
      width: 150px;
      font-weight: bold;
     
    }

    table {
      border-collapse: collapse;
      width: 100%;
      margin-top: 20px;
    }
    .container {
      display: inline-block;
      text-align: left;
    }

    table, th, td {
      border: 1px solid #ddd;
    }

    th, td {
      padding: 10px;
      text-align: left;
    }

    th {
      background-color: #f2f2f2;
    }
  </style>
</head>
<body>
<div class="container" style="margin-top: 3%; margin-bottom: 2%; align-items: center;">


  <div class="info-line">
    <div class="label">Registration Number:</div>
    <div><%=result.uid%></div>
    <div class="label" style="margin-left: 20px;">Date of Birth:</div>
    <div><%=result.dob%></div>
  </div>

  <div class="info-line">
    <div class="label">Candidate Name:</div>
    <div><%=result.name%></div>
    <div class="label" style="margin-left: 20px;">Course Duration:</div>
    <div><%=result.cdur%> Months</div>
  </div>

  <div class="info-line">
    <div class="label">Mother's Name:</div>
    <div><%=result.mname%></div>
    <div class="label" style="margin-left: 20px;">Father's Name:</div>
    <div><%=result.fname%></div>
  </div>

  <div class="info-line">
    <div class="label">Course Name:</div>
    <div><%=result.cname%></div>
  </div>

  <div class="info-line">
    <div class="label">Institute Name:</div>
    <div><%=result.iname%></div>
  </div>
  <div class="info-line">
    <div class="label">Result:</div>
    <div><%=result.status%></div>
  </div>
</div>
  <table>
    <thead>
      <tr>
        <th>SR. NO</th>
        <th>Subjects</th>
        <th>Marks Obtained</th>
        <th>Total Marks</th>
      </tr>
    </thead>
    <tbody>
        <% result.subjects.forEach((sub,index)=>{%>
            <tr>
                <td><%=index+1%></td>
                <td><%=sub.SubjectName%></td>
                <td><%=sub.SubjectNumber%></td>
                <td>100</td>
              </tr>
       <% })%>
     
     
    </tbody>
  </table>

</body>
</html>
